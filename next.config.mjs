/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[`lh3.googleusercontent.com`, `avatars.githubusercontent.com`, `cdn.discordapp.com`, `cdn.pixabay.com`, `images.unsplash.com`]
    }
};

export default nextConfig;
